const userModel = require('./users-model');
const bookModel = require('./books-model');

module.exports = {
    userModel,
    bookModel
};

//this file is used to export the userModel and bookModel so that they can be imported and used in other files. It allows for better organization and modularity of the code.